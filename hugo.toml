baseURL = 'https://narigin-liu.github.io/'
locale = 'en-us'
title = '成銀の世界'

theme = 'hugo-bearblog'

author = 'narigin'
copyright = 'Copyright © 2020, Jane Doe.'

# Generate a nice robots.txt for SEO
enableRobotsTXT = true

# Generate "Bearblog"-like URLs !only!, see https://bearblog.dev/.
disableKinds = ['taxonomy']
ignoreErrors = ['error-disable-taxonomy']
[permalinks]
    blog = '/:slug/'
    tags = '/blog/:slug'

# Also emit a search index on the home page (`/index.json`),
# consumed by the /search/ page.
[outputs]
    home = ['html', 'rss', 'json']

[params]
  # The "description" of your website. This is used in the meta data of your generated html.
  description = "Code for life."

  # The path to your "favicon". This should be a square (at least 32px x 32px) png-file.
  # Hint: It's good practise to also put a "favicon.ico"-file into your "static"-folder.
  favicon = "https://avatars.githubusercontent.com/u/176270325?v=4"

  # These "images" are used for the structured data templates. This will show up, when
  # services like Twitter or Slack want to generate a preview of a link to your site.
  # See https://gohugo.io/templates/internal#twitter-cards and
  # https://gohugo.io/templates/internal#open-graph.
  images = ["https://avatars.githubusercontent.com/u/176270325?v=4"]

  # Another "title" :-). This one is used as the site_name on the Hugo's internal
  # opengraph structured data template.
  # See https://ogp.me/ and https://gohugo.io/templates/internal#open-graph.
  title = "成銀の世界"

  # This theme will, by default, inject a made-with-line at the bottom of the page.
  # You can turn it off, but we would really appreciate if you don’t :-).
  hideMadeWithLine = true

  # By default, if you want to read more than one blog post you have to always go
  # back to the /blog page.
  # By enabling this feature each blog post will have at the end a navigator
  enablePostNavigator = true

  # By default, this theme displays dates with a format like "02 Jan, 2006", but
  # you can customize it by setting the `dateFormat` param in your site's config
  # file. See [Hugo's Format function docs](https://gohugo.io/functions/format/)
  # for details. An example TOML config that uses [ISO
  # 8601](https://en.wikipedia.org/wiki/ISO_8601) format:
  dateFormat = "2006-01-02"

  # enable LaTeX markup
  math = true

# Configure markup highlighter to match "Bearblog" aesthetics, see
# https://gohugo.io/configuration/markup/#highlight
[markup]
  [markup.highlight]
    style = 'friendly'
    lineNos = true
    lineNumbersInTable = false
    codeFences = true
    # Emit class-based markup; token colors are themed in
    # layouts/partials/custom_head.html via static/css/codeblock.css,
    # which swaps to a dark palette on prefers-color-scheme: dark.
    noClasses = false

  [markup.goldmark]
    [markup.goldmark.extensions]
      [markup.goldmark.extensions.passthrough]
        enable = true
        [markup.goldmark.extensions.passthrough.delimiters]
          block = [['\[', '\]'], ['$$', '$$']]
          inline = [['\(', '\)']]

[caches]
  [caches.images]
    dir = ':cacheDir/images'
